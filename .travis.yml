language: python
python:
  - "2.7"
install:
  - npm install less coffeescript
  - pip install .
before_script:
  - flake8 --ignore=E221,E701,E202,E123 freezr systemtests
services:
  - rabbitmq
script:
  - make actual-test
